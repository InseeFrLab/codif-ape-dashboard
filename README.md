# Dashboard monitoring de l'API de prédiction des codes APE

## Développement

Pour développer le dashboard, installer les dépendences (`pip install -r requirements.txt` et `source setup.sh`), puis modifier le fichier `index.qmd`.
Le dashboard se lance localement avec la commande `quarto serve index.qmd`.
