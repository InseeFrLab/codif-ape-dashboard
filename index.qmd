---
title: "Dashboard Test"
author: "MLBoss"
---

# Python

```{python}
#| output: false
import duckdb
from src import helpers

helpers.init_duckdb_s3()
helpers.read_all_data("s3://projet-ape/log_files/dashboard_test/**/*.parquet")
helpers.write_all_data_to_parquet()
```

## Row

```{python}
#| output: false
auto_percentages = duckdb.sql(
    """
    SELECT
        data."Query.Auto",
        COUNT(*) AS Occurrences,
        COUNT(*) * 100.0 / (SELECT COUNT(*) FROM data) AS Percentage
    FROM
        data
    GROUP BY
        data."Query.Auto";
    """
)
```

```{python}
#| output: false
percentage_high_ic = duckdb.sql(
    """
    SELECT
        COUNT(*) AS TotalRows,
        (COUNT(
            CASE WHEN data."Response.IC" > 0.8 THEN 1 END
            ) * 100.0 / COUNT(*)
        ) AS PercentageHighIC
    FROM data;
    """
).to_df().to_dict()
```

```{python}
#| component: valuebox
#| title: "Nombre de liasses"
dict(
    icon = "folder2-open",
    color = "secondary",
    value = percentage_high_ic["TotalRows"][0]
)
```


```{python}
#| component: valuebox
#| title: "Pourcentage de codification automatique"
dict(
    icon = "file-check",
    color = "success",
    value = f"""{round(percentage_high_ic["PercentageHighIC"][0],2)}%"""
)
```

## Row

```{python}
#| output: false
percentage_high_ic_day = duckdb.sql(
    """
    SELECT
        date,
        COUNT(*) AS TotalRows,
        (COUNT(
            CASE WHEN data."Response.IC" > 0.8 THEN 1 END
            ) * 100.0 / COUNT(*)
        ) AS PercentageHighIC
    FROM data
    GROUP BY date;
    """
).to_df()
```

```{python}
#| title: Nombre de requêtes par jour
#| padding: 0
helpers.make_barplot(
    percentage_high_ic_day,
    "date",
    "TotalRows")
```


```{python}
#| title: "Pourcentage de codification automatique par jour"
#| padding: 0
helpers.make_barplot(
    percentage_high_ic_day,
    "date",
    "PercentageHighIC",
    color="PercentageHighIC")
```

## Row

```{python}
#| output: false
percentage_high_ic_week = duckdb.sql(
    """
    SELECT
        DATE_TRUNC('week', date) AS week_start,
        COUNT(*) AS TotalRows,
        (COUNT(
            CASE WHEN data."Response.IC" > 0.8 THEN 1 END
            ) * 100.0 / COUNT(*)
        ) AS PercentageHighIC
    FROM data
    GROUP BY DATE_TRUNC('week', date);
    """
).to_df()
```

```{python}
#| title: Nombre de requêtes par semaine
#| padding: 0
helpers.make_barplot(
    percentage_high_ic_week,
    "week_start",
    "TotalRows")
```


```{python}
#| title: "Pourcentage de codification automatique par semaine"
#| padding: 0
helpers.make_barplot(
    percentage_high_ic_week,
    "week_start",
    "PercentageHighIC",
    color="PercentageHighIC")
```

### Column {width=100%}

```{python}
#| output: false
data = duckdb.sql("SELECT * FROM data").to_df()
```

```{python}
#| title: Distribution des indices de confiances
#| padding: 0
helpers.make_histogram(
    data,
    "Response.IC")
```

# Codes prédits

## Row

```{python}
#| output: false
npred_section = duckdb.sql(
    """
    SELECT
        LEFT("Response.1.code", 2) as section,
        COUNT(*) AS n_preds
    FROM data
    GROUP BY LEFT("Response.1.code", 2)
    """
).to_df()
```

```{python}
#| title: "Nombre de prédictions par section de la NAF"
#| padding: 0#|
helpers.make_barplot(
    npred_section,
    "section",
    "n_preds")
```

# Observable

## Row

```{ojs}
//| output: false

db = {
  const configuredClient = await DuckDBClient.of({
    data: FileAttachment("data/data.parquet")
  });
  return configuredClient;
}

data_month = db.query(`SELECT * FROM data`)
```

```{ojs}
data_month
```
